export const Form = () => {
    return (
        <form action="">
            <input type="text" placeholder="Insert the participants names" />
            <button disabled={true}>Add</button>
        </form>
    )
}